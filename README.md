# California Beans

A simple Docker test container project named "California Beans".

## Build

```bash
docker build -t california-beans .
```

## Run

```bash
docker run --rm -p 8080:8080 california-beans
```

Then visit `http://localhost:8080` to see the container response.
